<div class="container">
  <div class="row">
    <div class="col s12">
      <h3><%= t ".all_posts" %></h3>
        <%= paginate @posts %>
        <ul class="posts">
          <%= render @posts %>
        </ul>
        <%= paginate @posts %>
    </div>
  </div>
</div>

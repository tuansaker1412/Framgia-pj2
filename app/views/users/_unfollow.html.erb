<%= form_for current_user.active_relationships.find_by(followed_id: @user.id),
  html: {method: :delete} do |f| %>
  <%= f.submit t(".unfollow"), class: "unfollow-button btn grey", id: current_user.active_relationships.find_by(followed_id: @user.id).id %>
<% end %>
